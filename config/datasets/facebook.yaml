
name: "facebook"
data_path: "data/raw"

# Бинаризация (Facebook - implicit feedback, уже в бинарном виде)
binarize:
  rating_threshold: 0.0  # Не применяем фильтрацию по рейтингу для Facebook

filtering:
  min_user_interactions: 10
  min_item_interactions: 10

split:
  strategy: "temporal"
  seed: 42

graph:
  normalize: true
  self_loop: false
