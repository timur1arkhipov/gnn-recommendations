# Настройки датасета Yahoo (Music/Movies)

name: "yahoo"
data_path: "data/raw/yahoo"

# Бинаризация (преобразование в implicit feedback)
binarize:
  rating_threshold: 3.0  # Рейтинг >= 3 считается положительным
  # Для Yahoo Music: рейтинги обычно 1-5 или 1-100
  # Для Yahoo Movies: рейтинги обычно 1-5

# Фильтрация
filtering:
  min_user_interactions: 10
  min_item_interactions: 10

# Разделение данных (temporal per-user split)
split:
  strategy: "temporal"  # Разделение по времени для каждого пользователя
  seed: 42

# Граф
graph:
  normalize: true
  self_loop: false
