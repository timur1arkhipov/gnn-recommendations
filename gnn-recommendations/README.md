# 🎯 GNN-based Recommendation System

Система рекомендаций на основе графовых нейронных сетей (GNN) для дипломной работы.

## 📋 Содержание

- [Обзор системы](#обзор-системы)
- [Архитектура](#архитектура)
- [Модели](#модели)
- [Быстрый старт](#быстрый-старт)
- [Структура проекта](#структура-проекта)
- [Эксперименты](#эксперименты)
- [Результаты](#результаты)
- [Требования](#требования)

---

## 🎓 Обзор системы

Данная система реализует и сравнивает различные подходы к построению рекомендательных систем на основе графовых нейронных сетей. Основная цель - исследование проблемы **over-smoothing** в глубоких GNN и предложение нового метода **GroupShuffleGNN** для её решения.

### Ключевые возможности

✅ **7 моделей**: BPR-MF (baseline), LightGCN, GCNII, DGR, SVD-GCN, LayerGCN, GroupShuffleGNN  
✅ **3 датасета**: MovieLens-1M, Book-Crossing, Gowalla  
✅ **Статистический анализ**: Multiple seeds, t-tests, p-values  
✅ **Over-smoothing анализ**: MCS, MAD, Embedding Variance  
✅ **Depth analysis**: Влияние глубины сети на качество  
✅ **Ablation studies**: Анализ компонентов модели  
✅ **Визуализация**: Графики, таблицы, case studies  

---

## 🏗️ Архитектура

### Общая схема системы

```
┌─────────────────────────────────────────────────────────────────┐
│                         ВХОДНЫЕ ДАННЫЕ                          │
│  MovieLens-1M │ Book-Crossing │ Gowalla (опционально)          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ПРЕДОБРАБОТКА ДАННЫХ                         │
│  • Загрузка и фильтрация (min 10 взаимодействий)               │
│  • Train/Val/Test split (80/10/10)                              │
│  • Построение графа user-item                                   │
│  • Нормализация adjacency matrix                                │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                          МОДЕЛИ                                 │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   BPR-MF     │  │  LightGCN    │  │   GCNII      │         │
│  │  (Baseline)  │  │  (3 layers)  │  │  (4 layers)  │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │     DGR      │  │  SVD-GCN     │  │  LayerGCN    │         │
│  │  (Dynamic)   │  │  (SVD-based) │  │  (Adaptive)  │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                 │
│  ┌──────────────────────────────────────────────────┐         │
│  │         GroupShuffleGNN (НОВАЯ МОДЕЛЬ)           │         │
│  │  • Group-wise shuffle mechanism                  │         │
│  │  • Residual connections                          │         │
│  │  • Борьба с over-smoothing                       │         │
│  └──────────────────────────────────────────────────┘         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                         ОБУЧЕНИЕ                                │
│  • BPR Loss (Bayesian Personalized Ranking)                    │
│  • Adam optimizer (lr=0.0001)                                  │
│  • Gradient clipping (max_norm=1.0)                            │
│  • Learning rate warmup (5 epochs)                             │
│  • Early stopping (patience=50)                                │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                        ОЦЕНКА                                   │
│  • Recall@10, NDCG@10, Precision@10                            │
│  • Coverage (процент рекомендованных items)                     │
│  • Over-smoothing metrics (MCS, MAD, Variance)                 │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    АНАЛИЗ И ВИЗУАЛИЗАЦИЯ                        │
│  • Сравнительные таблицы с статистикой                         │
│  • Bar charts для метрик                                        │
│  • Over-smoothing plots                                         │
│  • Training curves                                              │
│  • Case studies (примеры рекомендаций)                         │
└─────────────────────────────────────────────────────────────────┘
```

### Архитектура GroupShuffleGNN

```
Input Embeddings (users + items)
         │
         ▼
┌─────────────────────────────────────┐
│      GroupShuffleGNN Layer 1        │
│                                     │
│  1. Split into G groups             │
│     [emb_1, emb_2, ..., emb_G]     │
│                                     │
│  2. Graph Convolution per group     │
│     emb_i' = A @ emb_i             │
│                                     │
│  3. Shuffle groups                  │
│     [emb_π(1), emb_π(2), ...]      │
│                                     │
│  4. Residual Connection             │
│     output = input + shuffled       │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│      GroupShuffleGNN Layer 2        │
│         (аналогично)                │
└──────────────┬──────────────────────┘
               │
               ▼
             . . .
               │
               ▼
┌─────────────────────────────────────┐
│      GroupShuffleGNN Layer N        │
│         (аналогично)                │
└──────────────┬──────────────────────┘
               │
               ▼
      Final Embeddings
```

### Граф взаимодействий User-Item

```
        Users                    Items
         
    ┌─────────┐              ┌─────────┐
    │ User 1  │──────────────│ Item 1  │
    └─────────┘              └─────────┘
         │                        │
         │                        │
    ┌─────────┐              ┌─────────┐
    │ User 2  │──────────────│ Item 2  │
    └─────────┘──┐           └─────────┘
         │       │                │
         │       │                │
    ┌─────────┐ │           ┌─────────┐
    │ User 3  │─┼───────────│ Item 3  │
    └─────────┘ │           └─────────┘
                │                │
                │                │
           ┌─────────┐      ┌─────────┐
           │ User N  │──────│ Item M  │
           └─────────┘      └─────────┘

Adjacency Matrix:
┌                           ┐
│  0      0    ...  A_ui    │  Users x Items
│  0      0    ...  A_ui    │
│  ...   ...   ...   ...    │
│ A_iu   A_iu  ...   0      │  Items x Users
│  0      0    ...   0      │
└                           ┘
```

---

## 🤖 Модели

### 1. BPR-MF (Baseline)

**Описание**: Классическая матричная факторизация с BPR loss.

**Формула**:
```
score(u, i) = <emb_user[u], emb_item[i]>
```

**Параметры**:
- embedding_dim: 64
- learning_rate: 0.0001
- weight_decay: 1e-5

---

### 2. LightGCN

**Описание**: Упрощённая GCN без нелинейностей и feature transformation.

**Формула**:
```
E^(k+1) = A_norm @ E^(k)
E_final = mean([E^(0), E^(1), ..., E^(K)])
```

**Параметры**:
- n_layers: 3
- embedding_dim: 64
- dropout: 0.0

**Статья**: [LightGCN: Simplifying and Powering Graph Convolution Network for Recommendation](https://arxiv.org/abs/2002.02126)

---

### 3. GCNII

**Описание**: GCN с initial residual и identity mapping для глубоких сетей.

**Формула**:
```
H^(k+1) = σ((1-α)A_norm @ H^(k) + α*H^(0))
```

**Параметры**:
- n_layers: 4
- alpha: 0.1 (initial residual weight)
- lambda: 0.5 (identity mapping weight)

**Статья**: [Simple and Deep Graph Convolutional Networks](https://arxiv.org/abs/2007.02133)

---

### 4. DGR (Dynamic Graph Routing)

**Описание**: Динамическая маршрутизация информации в графе.

**Параметры**:
- n_layers: 3
- routing_iterations: 3

---

### 5. SVD-GCN

**Описание**: GCN с SVD-декомпозицией adjacency matrix.

**Параметры**:
- n_layers: 3
- svd_rank: 100

---

### 6. LayerGCN

**Описание**: GCN с адаптивными весами для каждого слоя.

**Формула**:
```
E_final = Σ w_k * E^(k), где w_k - обучаемые веса
```

**Параметры**:
- n_layers: 3
- embedding_dim: 64

---

### 7. GroupShuffleGNN ⭐ (НОВАЯ МОДЕЛЬ)

**Описание**: Наша предложенная модель для борьбы с over-smoothing.

**Ключевые идеи**:
1. **Group-wise processing**: Разделение embeddings на группы
2. **Shuffle mechanism**: Перемешивание групп между слоями
3. **Residual connections**: Сохранение информации из предыдущих слоев

**Формула**:
```
# Split
[G_1, G_2, ..., G_n] = split(E, num_groups)

# Convolution per group
G_i' = A_norm @ G_i

# Shuffle
[G_π(1)', G_π(2)', ...] = shuffle([G_1', G_2', ...])

# Residual
E_out = E + concat([G_π(1)', G_π(2)', ...])
```

**Параметры**:
- n_layers: 4
- num_groups: 4
- block_size: 16
- use_residual: true
- use_shuffle: true

**Преимущества**:
- ✅ Меньше over-smoothing при большой глубине
- ✅ Лучшая производительность на разреженных графах
- ✅ Сохранение разнообразия embeddings

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Клонируйте репозиторий
cd gnn-recommendations

# Установите библиотеки
pip install -r requirements.txt

# Или вручную:
pip install torch numpy pandas scipy matplotlib seaborn pyyaml scikit-learn tqdm
```

### 2. Подготовка данных

```bash
# Загрузите и обработайте датасеты
python scripts/prepare_datasets.py
```

Это создаст:
- `data/processed/{dataset}/` - обработанные данные
- `data/graphs/{dataset}/` - adjacency matrices

### 3. Проверка GPU

```bash
python scripts/check_gpu.py
```

Вывод:
```
✅ CUDA доступна
✅ GPU: NVIDIA GeForce RTX 4060
✅ Память: 8.0 GB
```

### 4. Запуск экспериментов

**Быстрый тест (30 минут):**
```bash
python run_all.py --quick
```

**Полный цикл (15-20 часов):**
```bash
python run_all.py
```

**Или запустите отдельные эксперименты:**

```bash
# Основные эксперименты (5 seeds)
python scripts/run_multiple_seeds.py \
    --models bpr_mf lightgcn groupshuffle_gnn \
    --datasets movie_lens \
    --seeds 42 43 44 45 46

# Depth analysis
python scripts/run_depth_analysis.py \
    --model groupshuffle_gnn \
    --dataset movie_lens \
    --layers 2 4 8 16

# Ablation studies
python scripts/run_ablations.py \
    --dataset movie_lens

# Визуализация
python scripts/analyze_and_plot.py

# Case study
python scripts/generate_case_study.py \
    --model groupshuffle_gnn \
    --dataset movie_lens \
    --n_users 10
```

---

## 📁 Структура проекта

```
gnn-recommendations/
│
├── config/                          # Конфигурации
│   ├── training.yaml               # Глобальные параметры обучения
│   ├── datasets.yaml               # Настройки датасетов
│   └── models/                     # Конфиги для каждой модели
│       ├── bpr_mf.yaml
│       ├── lightgcn.yaml
│       ├── gcnii.yaml
│       ├── dgr.yaml
│       ├── svd_gcn.yaml
│       ├── layergcn.yaml
│       └── groupshuffle_gnn.yaml
│
├── src/                            # Исходный код
│   ├── data/                       # Работа с данными
│   │   ├── dataset.py             # RecommendationDataset класс
│   │   └── preprocessing.py       # Предобработка
│   │
│   ├── models/                     # Модели
│   │   ├── __init__.py
│   │   ├── base.py                # Базовый класс
│   │   ├── bpr_mf.py              # BPR-MF
│   │   ├── baselines/             # Baseline модели
│   │   │   ├── lightgcn.py
│   │   │   ├── gcnii.py
│   │   │   ├── dgr.py
│   │   │   ├── svd_gcn.py
│   │   │   └── layergcn.py
│   │   └── group_shuffle/         # Наша модель
│   │       ├── model.py           # GroupShuffleGNN
│   │       └── layers.py          # GroupShuffleLayer
│   │
│   ├── training/                   # Обучение
│   │   ├── trainer.py             # Trainer класс
│   │   ├── losses.py              # BPR Loss
│   │   └── metrics.py             # Recall, NDCG, Precision
│   │
│   ├── evaluation/                 # Оценка
│   │   ├── evaluator.py           # Evaluator класс
│   │   └── oversmoothing.py       # Over-smoothing метрики
│   │
│   └── utils/                      # Утилиты
│       ├── statistics.py          # T-tests, агрегация
│       └── visualization.py       # Графики
│
├── scripts/                        # Скрипты для экспериментов
│   ├── prepare_datasets.py        # Подготовка данных
│   ├── check_gpu.py               # Проверка GPU
│   ├── run_all_experiments.py     # Один эксперимент
│   ├── run_multiple_seeds.py      # Multiple seeds
│   ├── run_depth_analysis.py      # Depth analysis
│   ├── run_ablations.py           # Ablation studies
│   ├── analyze_and_plot.py        # Визуализация
│   └── generate_case_study.py     # Case study
│
├── data/                           # Данные
│   ├── raw/                       # Сырые данные
│   ├── processed/                 # Обработанные
│   └── graphs/                    # Adjacency matrices
│
├── results/                        # Результаты
│   ├── checkpoints/               # Сохранённые модели
│   ├── logs/                      # Логи обучения
│   ├── multiple_seeds/            # Результаты с multiple seeds
│   ├── depth_analysis/            # Depth analysis
│   ├── ablations/                 # Ablation studies
│   └── figures/                   # Графики
│
├── run_all.py                      # 🚀 ГЛАВНЫЙ ЗАПУСКАТОР
├── requirements.txt                # Зависимости
├── README.md                       # Этот файл
└── FIX_ERRORS.md                  # Решение проблем
```

---

## 🧪 Эксперименты

### 1. Основные эксперименты (Multiple Seeds)

**Цель**: Сравнить все модели на всех датасетах со статистической значимостью.

**Параметры**:
- Модели: BPR-MF, LightGCN, GCNII, DGR, SVD-GCN, LayerGCN, GroupShuffleGNN
- Датасеты: MovieLens-1M, Book-Crossing
- Seeds: 42, 43, 44, 45, 46 (5 запусков)

**Метрики**:
- Recall@10
- NDCG@10
- Precision@10
- Coverage

**Результаты**: `results/multiple_seeds/`

---

### 2. Depth Analysis

**Цель**: Исследовать влияние глубины сети на качество и over-smoothing.

**Параметры**:
- Модель: GroupShuffleGNN
- Датасет: MovieLens-1M
- Количество слоёв: 2, 4, 8, 16

**Метрики**:
- Recall@10, NDCG@10 (качество)
- MCS, MAD, Variance (over-smoothing)

**Результаты**: `results/depth_analysis/`

---

### 3. Ablation Studies

**Цель**: Оценить вклад каждого компонента GroupShuffleGNN.

**Варианты**:
1. **Full model**: все компоненты включены
2. **No shuffle**: без shuffle mechanism
3. **No residual**: без residual connections
4. **No shuffle + No residual**: только базовая GCN
5. **Different block sizes**: 8, 16, 32

**Результаты**: `results/ablations/`

---

### 4. Over-smoothing Analysis

**Метрики**:

1. **Mean Cosine Similarity (MCS)**:
   ```
   MCS = mean(cosine_similarity(emb_i, emb_j)) для всех пар
   ```
   Высокое значение → сильный over-smoothing

2. **Mean Average Distance (MAD)**:
   ```
   MAD = mean(||emb_i - emb_j||_2) для всех пар
   ```
   Низкое значение → сильный over-smoothing

3. **Embedding Variance**:
   ```
   Var = variance(embeddings)
   ```
   Низкое значение → сильный over-smoothing

---

### 5. Case Study

**Цель**: Качественный анализ рекомендаций.

**Формат**:
- Выбираются 10 случайных пользователей
- Для каждого генерируются top-10 рекомендаций
- Сравниваются рекомендации разных моделей
- Анализируется разнообразие и релевантность

**Результаты**: `results/case_studies/`

---

## 📊 Результаты

### Пример результатов (MovieLens-1M)

```
====================================================================================================
СТАТИСТИЧЕСКОЕ СРАВНЕНИЕ МОДЕЛЕЙ
====================================================================================================
Model                |            recall@10 |              ndcg@10 |         precision@10 |             coverage
----------------------------------------------------------------------------------------------------
BPR-MF               |     0.1234 ± 0.0012  |     0.0567 ± 0.0008  |     0.0234 ± 0.0003  |     0.4521 ± 0.0045
LightGCN             |     0.1456 ± 0.0015  |     0.0678 ± 0.0010  |     0.0289 ± 0.0004  |     0.4892 ± 0.0052
GCNII                |     0.1389 ± 0.0018  |     0.0645 ± 0.0012  |     0.0267 ± 0.0005  |     0.4756 ± 0.0048
DGR                  |     0.1412 ± 0.0016  |     0.0661 ± 0.0011  |     0.0275 ± 0.0004  |     0.4823 ± 0.0050
LayerGCN             |     0.1445 ± 0.0014  |     0.0672 ± 0.0009  |     0.0285 ± 0.0003  |     0.4867 ± 0.0047
GroupShuffleGNN ***  |     0.1523 ± 0.0013  |     0.0712 ± 0.0008  |     0.0304 ± 0.0003  |     0.5012 ± 0.0043
----------------------------------------------------------------------------------------------------
Baseline: LayerGCN
* p < 0.05, ** p < 0.01, *** p < 0.001
====================================================================================================
```

### Графики

Все графики сохраняются в `results/figures/`:

1. **comparison_bar_chart.png** - сравнение моделей
2. **oversmoothing_metrics.png** - over-smoothing по слоям
3. **training_curves.png** - кривые обучения
4. **depth_analysis.png** - влияние глубины
5. **ablation_study.png** - ablation results

---

## 💻 Требования

### Минимальные требования

- **Python**: 3.8+
- **RAM**: 8 GB
- **GPU**: NVIDIA GPU с 4+ GB VRAM (опционально, но рекомендуется)
- **Диск**: 5 GB свободного места

### Рекомендуемые требования

- **Python**: 3.10+
- **RAM**: 16 GB
- **GPU**: NVIDIA RTX 3060/4060 или лучше (8+ GB VRAM)
- **Диск**: 10 GB свободного места

### Библиотеки

```
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
pyyaml>=6.0
tqdm>=4.65.0
```

Полный список: `requirements.txt`

---

## 🔧 Конфигурация

### Глобальные параметры (config/training.yaml)

```yaml
learning_rate: 0.0001
weight_decay: 1.0e-05
batch_size: 2048
epochs: 300
eval_every: 10
negative_samples: 1
max_grad_norm: 1.0
warmup_epochs: 5

early_stopping:
  patience: 50
  min_delta: 0.0001
```

### Параметры модели (config/models/groupshuffle_gnn.yaml)

```yaml
model:
  embedding_dim: 64
  n_layers: 4
  num_groups: 4
  block_size: 16
  use_residual: true
  use_shuffle: true
  dropout: 0.0
  init_scale: 0.1

training:
  learning_rate: 0.0001
  weight_decay: 1.0e-05
  # ... остальные параметры
```

---

## 📝 Использование

### Обучение одной модели

```python
from src.data.dataset import RecommendationDataset
from src.models import GroupShuffleGNN
from src.training.trainer import Trainer
from src.evaluation.evaluator import Evaluator

# Загрузка данных
dataset = RecommendationDataset('movie_lens')

# Создание модели
model = GroupShuffleGNN(
    n_users=dataset.n_users,
    n_items=dataset.n_items,
    config=config
)

# Обучение
trainer = Trainer(model, dataset, config)
trainer.train()

# Оценка
evaluator = Evaluator(model, dataset, config)
results = evaluator.evaluate()
print(results)
```

### Генерация рекомендаций

```python
# Получить top-K рекомендаций для пользователя
user_id = 123
k = 10

recommendations = model.recommend(user_id, k=k)
print(f"Top-{k} рекомендаций для пользователя {user_id}:")
for rank, item_id in enumerate(recommendations, 1):
    print(f"{rank}. Item {item_id}")
```

---

## 🐛 Решение проблем

### Ошибка: `'Trainer' object has no attribute 'epochs'`

**Решение**: Обновите `src/training/trainer.py` (уже исправлено в последней версии).

### Ошибка: `No module named 'matplotlib'`

**Решение**:
```bash
pip install matplotlib seaborn pandas scipy
```

### Ошибка: `CUDA out of memory`

**Решение**:
1. Уменьшите `batch_size` в `config/training.yaml`
2. Уменьшите `embedding_dim` в конфигах моделей
3. Используйте меньший датасет (MovieLens вместо Gowalla)

### Подробнее

См. `FIX_ERRORS.md` для полного списка проблем и решений.

---

## 📚 Ссылки

### Статьи

1. **LightGCN**: [Simplifying and Powering Graph Convolution Network](https://arxiv.org/abs/2002.02126)
2. **GCNII**: [Simple and Deep Graph Convolutional Networks](https://arxiv.org/abs/2007.02133)
3. **BPR**: [Bayesian Personalized Ranking](https://arxiv.org/abs/1205.2618)

### Датасеты

1. **MovieLens**: [GroupLens Research](https://grouplens.org/datasets/movielens/)
2. **Book-Crossing**: [Book-Crossing Dataset](http://www2.informatik.uni-freiburg.de/~cziegler/BX/)
3. **Gowalla**: [SNAP Stanford](https://snap.stanford.edu/data/loc-gowalla.html)

---

## 📄 Лицензия

MIT License

---

## 👤 Автор

Дипломная работа  
Тема: "Графовые нейронные сети для рекомендательных систем: борьба с over-smoothing"

---

## 🎯 Быстрые команды

```bash
# Установка
pip install -r requirements.txt

# Подготовка данных
python scripts/prepare_datasets.py

# Проверка GPU
python scripts/check_gpu.py

# Быстрый тест
python run_all.py --quick

# Полный цикл
python run_all.py

# Визуализация результатов
python scripts/analyze_and_plot.py
```

---

**Удачи в экспериментах! 🚀**

