# Модуль подготовки данных

Этот модуль содержит все необходимые компоненты для подготовки данных рекомендательных систем.

## 🎯 Два способа использования

### 1. Автоматический (рекомендуется) ✅

**Один скрипт делает всё автоматически:**

```bash
# Подготовка MovieLens
python scripts/prepare_data.py --dataset movie_lens

# Подготовка Book-Crossing
python scripts/prepare_data.py --dataset book_crossing

# Подготовка Gowalla
python scripts/prepare_data.py --dataset gowalla
```

**Что происходит внутри:**
1. Загружает сырые данные (через соответствующий загрузчик)
2. Выполняет препроцессинг (фильтрация, бинаризация, нормализация)
3. Разделяет на train/valid/test
4. Строит графы
5. Сохраняет все результаты

**Вы просто запускаете одну команду - всё остальное автоматически!**

### 2. Ручной (для кастомизации)

Если нужно настроить параметры:

```python
from data import RecommendationDataset

# Создаем объект датасета
dataset = RecommendationDataset(name="movie_lens", root_dir=".")

# 1. Загружаем сырые данные
dataset.load_raw_data()

# 2. Препроцессинг (можно настроить параметры)
dataset.preprocess(
    min_user_interactions=20,  # кастомная фильтрация
    min_item_interactions=20,
    rating_threshold=3.0
)

# 3. Разделение на train/valid/test
dataset.split(
    strategy='random',  # или 'temporal'
    train_ratio=0.8,
    valid_ratio=0.1,
    test_ratio=0.1
)

# 4. Построение графов
dataset.build_graph()

# 5. Получаем граф для обучения
adj_matrix = dataset.get_torch_adjacency(normalized=True)
```

---

## 📊 ПОТОК ВЫПОЛНЕНИЯ ПРОГРАММЫ

### Полный pipeline (автоматический)

```
scripts/prepare_data.py
    ↓
RecommendationDataset.__init__()
    ├── Загружает конфигурацию из config/datasets/{name}.yaml
    └── Инициализирует пути к данным
    ↓
dataset.load_raw_data()
    ├── get_loader(dataset_name) → получает загрузчик для датасета
    │   ├── MovieLensLoader → для movie_lens
    │   ├── BookCrossingLoader → для book_crossing
    │   └── GowallaLoader → для gowalla
    │
    └── loader.load(data_path) → загружает и преобразует в единый формат
        └── Возвращает DataFrame: userId, itemId, rating?, timestamp?
    ↓
dataset.preprocess()
    ├── remove_duplicates() → удаляет дубликаты
    ├── filter_by_min_interactions() → фильтрует по минимальному количеству взаимодействий
    ├── binarize_interactions() → бинаризует (implicit feedback)
    └── normalize_ids() → нормализует ID (0, 1, 2, ...)
    ↓
dataset.split()
    ├── temporal split → по timestamp (если есть)
    └── random split → случайное разделение
    └── Сохраняет train.txt, valid.txt, test.txt
    ↓
dataset.build_graph()
    ├── build_bipartite_graph() → строит bipartite граф
    ├── normalize_adjacency_matrix() → нормализует adjacency matrix
    └── Сохраняет adj_matrix.npz, norm_adj_matrix.npz
    ↓
✅ ГОТОВО! Данные подготовлены для обучения моделей
```

---

## 🏗️ АРХИТЕКТУРА: Как код отличается для разных датасетов

### Принцип: Разные форматы на входе → Единый формат после загрузки

```
┌─────────────────────────────────────────────────────────────┐
│                    РАЗНЫЕ ДАТАСЕТЫ                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  MovieLens:          Book-Crossing:        Gowalla:          │
│  ratings.csv         BX-Book-Ratings.csv   Gowalla_*.csv     │
│  userId, movieId     User-ID, ISBN         userId, itemId    │
│  rating, timestamp   Book-Rating           timestamp         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │   СИСТЕМА ЗАГРУЗЧИКОВ          │
        │   (loaders.py)                 │
        ├───────────────────────────────┤
        │  MovieLensLoader               │
        │  BookCrossingLoader            │
        │  GowallaLoader                 │
        └───────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │   ЕДИНЫЙ ФОРМАТ                │
        │   DataFrame:                   │
        │   - userId (int)               │
        │   - itemId (int/str)           │
        │   - rating? (float)             │
        │   - timestamp? (int)           │
        └───────────────────────────────┘
                        ↓
        ┌───────────────────────────────┐
        │   ЕДИНЫЙ PIPELINE              │
        │   (одинаковый для всех!)       │
        ├───────────────────────────────┤
        │  preprocessing.py              │
        │  graph_builder.py              │
        │  dataset.py                    │
        └───────────────────────────────┘
```

### Ключевой момент:

**После загрузки все датасеты имеют одинаковый формат!**

Это означает:
- ✅ Один и тот же код препроцессинга для всех датасетов
- ✅ Один и тот же код построения графов для всех датасетов
- ✅ Один и тот же код обучения моделей для всех датасетов

**Различия только в загрузчиках** (`loaders.py`), которые знают формат конкретного датасета.

---

## 📁 КОМПОНЕНТЫ МОДУЛЯ

### 1. `loaders.py` - Система загрузчиков

**Назначение:** Загружает данные разных форматов и преобразует в единый формат.

**Структура:**
```python
BaseDatasetLoader (абстрактный класс)
    ├── MovieLensLoader
    │   └── load() → загружает ratings.csv, преобразует movieId → itemId
    │
    ├── BookCrossingLoader
    │   └── load() → загружает BX-Book-Ratings.csv, обрабатывает рейтинг 0
    │
    └── GowallaLoader
        └── load() → загружает Gowalla_*.csv или *.txt, определяет формат автоматически
```

**Как работает:**
- Каждый загрузчик знает формат своего датасета
- Преобразует колонки в единый формат: `userId`, `itemId`, `rating?`, `timestamp?`
- После загрузки все датасеты выглядят одинаково

**Регистр загрузчиков:**
```python
LOADER_REGISTRY = {
    'movie_lens': MovieLensLoader,
    'book_crossing': BookCrossingLoader,
    'book-crossing': BookCrossingLoader,  # альтернативное имя
    'gowalla': GowallaLoader,
}
```

### 2. `preprocessing.py` - Препроцессинг

**Назначение:** Обрабатывает данные в едином формате (одинаково для всех датасетов).

**Функции:**
- `remove_duplicates()` - удаляет дубликаты взаимодействий
- `filter_by_min_interactions()` - фильтрует пользователей/айтемы по минимальному количеству взаимодействий
- `binarize_interactions()` - бинаризует взаимодействия (implicit feedback)
  - Для Book-Crossing: рейтинг 0 = implicit feedback (пользователь прочитал, но не оценил)
  - Для других: рейтинг > threshold = положительное взаимодействие
- `normalize_ids()` - нормализует ID (приводит к 0, 1, 2, ...)
  - Поддерживает как числовые, так и строковые ID (например, ISBN)
- `get_statistics()` - вычисляет статистику датасета

**Особенности:**
- Работает с единым форматом (userId, itemId)
- Не зависит от исходного формата датасета
- Одинаковый код для всех датасетов

### 3. `graph_builder.py` - Построение графов

**Назначение:** Строит bipartite графы для GNN моделей.

**Функции:**
- `build_bipartite_graph()` - строит bipartite граф user-item
- `normalize_adjacency_matrix()` - нормализует adjacency matrix (symmetric normalization)
- `convert_to_torch_sparse()` - преобразует в PyTorch sparse tensor
- `save_adjacency_matrix()` / `load_adjacency_matrix()` - сохранение/загрузка

**Особенности:**
- Работает с единым форматом (userId, itemId)
- Одинаковый код для всех датасетов

### 4. `dataset.py` - Основной класс

**Назначение:** Оркестрирует весь pipeline подготовки данных.

**Класс:** `RecommendationDataset`

**Методы:**
```python
__init__(name, root_dir)      # Инициализация, загрузка конфигурации
load_raw_data()                # Загрузка сырых данных (через загрузчики)
preprocess()                   # Препроцессинг (фильтрация, бинаризация, нормализация)
split()                        # Разделение на train/valid/test
build_graph()                  # Построение графов
load_processed_data()          # Загрузка уже обработанных данных
get_torch_adjacency()          # Получение графа для обучения
```

**Как работает:**
1. При создании загружает конфигурацию из `config/datasets/{name}.yaml`
2. `load_raw_data()` использует систему загрузчиков для загрузки данных
3. Все остальные методы работают с единым форматом

---

## 🔄 ПОСЛЕДОВАТЕЛЬНОСТЬ ВЫЗОВОВ

### Автоматический режим (рекомендуется)

```bash
python scripts/prepare_data.py --dataset book_crossing
```

**Внутри скрипта:**
```python
# scripts/prepare_data.py
def prepare_dataset(dataset_name, root_dir):
    # 1. Создаем объект датасета
    dataset = RecommendationDataset(name=dataset_name, root_dir=root_dir)
    
    # 2. Загружаем сырые данные
    dataset.load_raw_data()  # ← использует загрузчик для конкретного датасета
    
    # 3. Препроцессинг
    dataset.preprocess()     # ← единый код для всех датасетов
    
    # 4. Разделение
    dataset.split()          # ← единый код для всех датасетов
    
    # 5. Построение графов
    dataset.build_graph()    # ← единый код для всех датасетов
    
    return dataset
```

### Ручной режим (для кастомизации)

```python
from data import RecommendationDataset

# 1. Инициализация
dataset = RecommendationDataset(name="book_crossing", root_dir=".")
#    ↓ Загружает config/datasets/book_crossing.yaml

# 2. Загрузка данных
dataset.load_raw_data()
#    ↓ get_loader("book_crossing") → BookCrossingLoader
#    ↓ loader.load() → загружает BX-Book-Ratings.csv
#    ↓ Преобразует в единый формат: userId, itemId, rating

# 3. Препроцессинг
dataset.preprocess()
#    ↓ remove_duplicates()
#    ↓ filter_by_min_interactions()
#    ↓ binarize_interactions() → обрабатывает рейтинг 0 для Book-Crossing
#    ↓ normalize_ids() → нормализует ID (поддерживает строковые ISBN)

# 4. Разделение
dataset.split()
#    ↓ temporal или random split
#    ↓ Сохраняет train.txt, valid.txt, test.txt

# 5. Построение графов
dataset.build_graph()
#    ↓ build_bipartite_graph()
#    ↓ normalize_adjacency_matrix()
#    ↓ Сохраняет adj_matrix.npz, norm_adj_matrix.npz
```

---

## 📚 ПОДДЕРЖИВАЕМЫЕ ДАТАСЕТЫ

### MovieLens
- **Формат:** `ratings.csv`
- **Колонки:** `userId`, `movieId`, `rating`, `timestamp`
- **Путь:** `data/raw/movie_lens/ratings.csv`
- **Загрузчик:** `MovieLensLoader`
- **Особенности:** Преобразует `movieId` → `itemId`

### Book-Crossing
- **Формат:** `BX-Book-Ratings.csv`
- **Колонки:** `User-ID`, `ISBN`, `Book-Rating`
- **Разделитель:** `;` (точка с запятой)
- **Путь:** `data/raw/book-crossing/BX-Book-Ratings.csv`
- **Загрузчик:** `BookCrossingLoader`
- **Особенности:**
  - Рейтинг 0 = implicit feedback (пользователь прочитал, но не оценил)
  - ISBN может быть строкой (например, "034545104X")
  - Нормализация ID поддерживает строковые значения

### Gowalla
- **Формат:** `Gowalla_cleanCheckins.csv` или `Gowalla_edges.txt`
- **Колонки:** `userId`, `itemId` (locationId), `timestamp?`
- **Путь:** `data/raw/gowalla/`
- **Загрузчик:** `GowallaLoader`
- **Особенности:**
  - Автоматическое определение формата
  - Эффективная обработка больших файлов (батчами)
  - Поддержка разных разделителей

---

## 📂 СТРУКТУРА ОБРАБОТАННЫХ ДАННЫХ

После обработки данные сохраняются в:

### `data/processed/{dataset_name}/`
- `train.txt` - обучающая выборка (формат: `userId itemId`)
- `valid.txt` - валидационная выборка
- `test.txt` - тестовая выборка
- `stats.json` - статистика датасета

### `data/graphs/{dataset_name}/`
- `adj_matrix.npz` - adjacency matrix (scipy sparse format)
- `norm_adj_matrix.npz` - нормализованная adjacency matrix

---

## 💡 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Автоматическая подготовка

```bash
# Просто запустите скрипт - всё сделается автоматически
python scripts/prepare_data.py --dataset book_crossing
```

### Пример 2: Загрузка уже обработанных данных

```python
from data import RecommendationDataset

# Загружаем уже обработанные данные
dataset = RecommendationDataset(name="book_crossing")
dataset.load_processed_data()  # Загружает из data/processed/book_crossing/

# Получаем граф для обучения
adj_matrix = dataset.get_torch_adjacency(normalized=True)
# adj_matrix - это PyTorch sparse tensor, готовый для GNN моделей
```

### Пример 3: Кастомные параметры

```python
from data import RecommendationDataset

dataset = RecommendationDataset(name="book_crossing")

# Загружаем данные
dataset.load_raw_data()

# Кастомная фильтрация (более строгая)
dataset.preprocess(
    min_user_interactions=20,  # минимум 20 взаимодействий для пользователя
    min_item_interactions=20,  # минимум 20 взаимодействий для айтема
    rating_threshold=0.0        # для Book-Crossing: 0 = implicit feedback
)

# Кастомное разделение
dataset.split(
    strategy='random',      # случайное разделение (вместо temporal)
    train_ratio=0.8,
    valid_ratio=0.1,
    test_ratio=0.1
)

# Строим графы
dataset.build_graph()
```

### Пример 4: Добавление нового датасета

```python
# 1. Создаем загрузчик в loaders.py
class MyDatasetLoader(BaseDatasetLoader):
    def load(self, data_path: Path) -> pd.DataFrame:
        # Загружаем данные в специфичном формате
        df = pd.read_csv(data_path / "my_data.csv")
        
        # Преобразуем в единый формат
        df = self._normalize_columns(df, {
            'userId': 'user_id',
            'itemId': 'product_id',
            'rating': 'score'
        })
        
        return df

# 2. Регистрируем загрузчик
LOADER_REGISTRY['my_dataset'] = MyDatasetLoader

# 3. Создаем конфигурацию
# config/datasets/my_dataset.yaml

# 4. Используем как обычно
python scripts/prepare_data.py --dataset my_dataset
```

---

## ✅ ИТОГ

**Главное:**

1. **Один скрипт делает всё:** `python scripts/prepare_data.py --dataset {name}`
2. **Разные форматы на входе → Единый формат после загрузки**
3. **Один и тот же код для всех датасетов** (после загрузки)
4. **Различия только в загрузчиках** (они знают формат конкретного датасета)

**Вы просто запускаете одну команду - всё остальное автоматически!** 🚀

