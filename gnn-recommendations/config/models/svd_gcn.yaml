# Конфигурация для SVD-GCN модели

model_name: "svd_gcn"

# Параметры модели
model:
  embedding_dim: 64
  n_layers: 3
  rank: 64            # Ранг для SVD (если None, используется embedding_dim)
  dropout: 0.0
  init_scale: 0.1  # Увеличено с 0.01 для лучшей инициализации

