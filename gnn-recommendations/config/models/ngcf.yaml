# Конфигурация для NGCF модели

model_name: "ngcf"

# Параметры модели
model:
  embedding_dim: 64
  layer_sizes: [64, 64, 64]  # Размерности для каждого из 3 слоев
  dropout: 0.1  # Dropout для регуляризации
  init_scale: 0.01  # Масштаб инициализации
