# ⚡ Быстрый старт

Пошаговая инструкция для запуска экспериментов за 5 минут.

---

## 📋 Чек-лист

- [ ] Python 3.8+ установлен
- [ ] GPU с CUDA (опционально, но рекомендуется)
- [ ] 8+ GB RAM
- [ ] 5+ GB свободного места на диске

---

## 🚀 Шаг 1: Установка зависимостей

### Windows

```bash
# Перейдите в директорию проекта
cd gnn-recommendations

# Установите библиотеки
pip install -r requirements.txt

# Или вручную:
pip install torch numpy pandas scipy matplotlib seaborn pyyaml scikit-learn tqdm
```

### Linux/MacOS

```bash
cd gnn-recommendations
pip3 install -r requirements.txt
```

---

## 🔍 Шаг 2: Проверка GPU

```bash
python scripts/check_gpu.py
```

**Ожидаемый вывод**:
```
✅ CUDA доступна
✅ GPU: NVIDIA GeForce RTX 4060
✅ Память: 8.0 GB
```

Если GPU нет:
```
⚠️  CUDA недоступна. Обучение будет на CPU (медленнее).
```

---

## 📊 Шаг 3: Подготовка данных

```bash
python scripts/prepare_datasets.py
```

Это займёт 2-5 минут. Будут созданы:
- `data/processed/movie_lens/` - обработанные данные
- `data/processed/book_crossing/`
- `data/graphs/movie_lens/` - adjacency matrices
- `data/graphs/book_crossing/`

**Ожидаемый вывод**:
```
================================================================================
ПОДГОТОВКА ДАТАСЕТА: movie_lens
================================================================================
Загрузка данных...
Фильтрация пользователей и items (min 10 взаимодействий)...
Статистика:
  Пользователей: 6040
  Items: 3706
  Взаимодействий: 1000209
Разделение на train/val/test...
Сохранение...
✅ Датасет movie_lens готов!
```

---

## 🎯 Шаг 4: Запуск экспериментов

### Вариант A: Быстрый тест (30 минут)

```bash
python run_all.py --quick
```

**Что запустится**:
- 3 модели: BPR-MF, LightGCN, GroupShuffleGNN
- 1 датасет: MovieLens-1M
- 2 seeds: 42, 43
- Без depth analysis и ablations

### Вариант B: Полный цикл (15-20 часов)

```bash
python run_all.py
```

**Что запустится**:
- 7 моделей: BPR-MF, LightGCN, GCNII, DGR, SVD-GCN, LayerGCN, GroupShuffleGNN
- 2 датасета: MovieLens-1M, Book-Crossing
- 5 seeds: 42, 43, 44, 45, 46
- Depth analysis (4 варианта глубины)
- Ablation studies (5 вариантов)
- Визуализация
- Case study

---

## 📈 Шаг 5: Просмотр результатов

После завершения экспериментов результаты будут в:

```
results/
├── multiple_seeds/
│   ├── movie_lens_aggregated.json      # Агрегированные метрики
│   ├── book_crossing_aggregated.json
│   └── all_results_multiple_seeds.json
│
├── depth_analysis/
│   └── groupshuffle_gnn_movie_lens_depth.json
│
├── ablations/
│   └── groupshuffle_gnn_movie_lens_ablations.json
│
└── figures/                             # Графики (PNG)
    ├── comparison_bar_chart.png
    ├── oversmoothing_metrics.png
    ├── training_curves.png
    ├── depth_analysis.png
    └── ablation_study.png
```

### Просмотр таблицы результатов

Откройте `results/multiple_seeds/movie_lens_aggregated.json`:

```json
{
  "bpr_mf": {
    "recall@10": {"mean": 0.1234, "std": 0.0012},
    "ndcg@10": {"mean": 0.0567, "std": 0.0008},
    ...
  },
  "groupshuffle_gnn": {
    "recall@10": {"mean": 0.1523, "std": 0.0013},
    "ndcg@10": {"mean": 0.0712, "std": 0.0008},
    ...
  }
}
```

### Просмотр графиков

Откройте файлы в `results/figures/` любым просмотрщиком изображений.

---

## 🔧 Альтернативные команды

### Запуск только основных экспериментов

```bash
python scripts/run_multiple_seeds.py \
    --models bpr_mf lightgcn groupshuffle_gnn \
    --datasets movie_lens \
    --seeds 42 43 44 45 46
```

### Запуск только depth analysis

```bash
python scripts/run_depth_analysis.py \
    --model groupshuffle_gnn \
    --dataset movie_lens \
    --layers 2 4 8 16
```

### Запуск только ablation studies

```bash
python scripts/run_ablations.py \
    --dataset movie_lens
```

### Только визуализация (если эксперименты уже выполнены)

```bash
python scripts/analyze_and_plot.py \
    --results_dir results/multiple_seeds \
    --output_dir results/figures
```

### Генерация case study

```bash
python scripts/generate_case_study.py \
    --model groupshuffle_gnn \
    --dataset movie_lens \
    --n_users 10
```

---

## 🐛 Решение проблем

### Ошибка: `'Trainer' object has no attribute 'epochs'`

**Решение**: Файл уже исправлен. Если ошибка всё ещё есть, обновите репозиторий.

### Ошибка: `No module named 'matplotlib'`

**Решение**:
```bash
pip install matplotlib seaborn pandas scipy
```

### Ошибка: `CUDA out of memory`

**Решение 1** - Уменьшите batch size в `config/training.yaml`:
```yaml
batch_size: 1024  # было 2048
```

**Решение 2** - Используйте только MovieLens (меньше данных):
```bash
python run_all.py --quick
```

**Решение 3** - Исключите Gowalla (слишком большой):
```bash
python scripts/run_multiple_seeds.py \
    --models bpr_mf lightgcn \
    --datasets movie_lens book_crossing \
    --seeds 42 43
```

### Ошибка: `ImportError: cannot import name ...`

**Решение**: Убедитесь, что запускаете из правильной директории:
```bash
cd gnn-recommendations
python run_all.py
```

### Ошибка: `FileNotFoundError: data/processed/...`

**Решение**: Сначала подготовьте данные:
```bash
python scripts/prepare_datasets.py
```

---

## 📊 Ожидаемые результаты

### Время выполнения

| Эксперимент | Быстрый режим | Полный режим |
|-------------|---------------|--------------|
| Multiple Seeds | 20 мин | 10-12 часов |
| Depth Analysis | - | 2-3 часа |
| Ablation Studies | - | 1-2 часа |
| Визуализация | 5 мин | 10 мин |
| Case Study | 5 мин | 10 мин |
| **ИТОГО** | **~30 мин** | **~15-20 часов** |

### Метрики качества (MovieLens-1M)

| Модель | Recall@10 | NDCG@10 | Precision@10 |
|--------|-----------|---------|--------------|
| BPR-MF | 0.123 | 0.057 | 0.023 |
| LightGCN | 0.146 | 0.068 | 0.029 |
| GCNII | 0.139 | 0.065 | 0.027 |
| **GroupShuffleGNN** | **0.152** | **0.071** | **0.030** |

---

## 🎓 Следующие шаги

1. ✅ Запустите быстрый тест: `python run_all.py --quick`
2. ✅ Проверьте результаты в `results/figures/`
3. ✅ Если всё работает, запустите полный цикл: `python run_all.py`
4. ✅ Изучите подробную документацию: `README.md`
5. ✅ Посмотрите архитектуру: `ARCHITECTURE.md`

---

## 📚 Полезные ссылки

- **README.md** - Полная документация
- **ARCHITECTURE.md** - Архитектура с диаграммами
- **FIX_ERRORS.md** - Решение проблем
- **HOW_TO_TRAIN.md** - Детали обучения
- **SYSTEM_OVERVIEW.md** - Обзор системы

---

## 💡 Советы

1. **Начните с быстрого теста** - убедитесь, что всё работает
2. **Используйте GPU** - в 10-20 раз быстрее
3. **Мониторьте память** - если GPU переполняется, уменьшите batch_size
4. **Сохраняйте результаты** - они понадобятся для дипломной работы
5. **Изучайте графики** - они покажут, работает ли модель правильно

---

## 🎯 Минимальный пример

Если хотите просто посмотреть, как работает система:

```bash
# 1. Установка
pip install torch numpy pandas scipy matplotlib seaborn pyyaml scikit-learn tqdm

# 2. Подготовка данных
python scripts/prepare_datasets.py

# 3. Быстрый тест
python run_all.py --quick

# 4. Просмотр результатов
# Откройте results/figures/comparison_bar_chart.png
```

**Время**: ~40 минут  
**Результат**: Сравнение 3 моделей на MovieLens-1M

---

**Готово! Удачи в экспериментах! 🚀**
