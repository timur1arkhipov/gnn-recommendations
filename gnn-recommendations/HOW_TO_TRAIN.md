# 🚀 Как начать обучение моделей

## 📋 Быстрый старт

### Шаг 1: Подготовка данных (если еще не сделано)

```bash
# Подготовка датасета MovieLens
python scripts/prepare_data.py --dataset movie_lens

# Подготовка датасета Book-Crossing
python scripts/prepare_data.py --dataset book_crossing

# Подготовка датасета Gowalla
python scripts/prepare_data.py --dataset gowalla
```

**Результат:** Данные будут сохранены в `data/processed/{dataset}/` и `data/graphs/{dataset}/`

---

### Шаг 2: Обучение модели

```bash
# Обучение LightGCN на MovieLens
python scripts/train_model.py --model lightgcn --dataset movie_lens

# Обучение GroupShuffleGNN на Book-Crossing
python scripts/train_model.py --model groupshuffle_gnn --dataset book_crossing

# Обучение с другим seed
python scripts/train_model.py --model lightgcn --dataset movie_lens --seed 123
```

---

## 📊 Доступные модели

| Модель | Название в скрипте | Описание |
|--------|-------------------|----------|
| **BPR-MF** | `bpr_mf` | Базовый метод матричной факторизации |
| **LightGCN** | `lightgcn` | Упрощенный GCN (рекомендуется для начала) |
| **GCNII** | `gcnii` | GCN с residual connections |
| **DGR** | `dgr` | Desmoothing framework |
| **SVD-GCN** | `svd_gcn` | GCN с SVD декомпозицией |
| **LayerGCN** | `layergcn` | Layer-wise refinement |
| **GroupShuffleGNN** ⭐ | `groupshuffle_gnn` | Ваша модель с ортогональными преобразованиями |

---

## 📁 Доступные датасеты

| Датасет | Название в скрипте |
|---------|-------------------|
| **MovieLens** | `movie_lens` |
| **Book-Crossing** | `book_crossing` или `book-crossing` |
| **Gowalla** | `gowalla` |

---

## ⚙️ Настройка параметров обучения

### Через конфигурационные файлы

Параметры обучения настраиваются в:
- `config/training.yaml` - общие настройки для всех моделей
- `config/models/{model_name}.yaml` - специфичные настройки для модели

**Пример:** `config/training.yaml`
```yaml
learning_rate: 0.001
batch_size: 2048
epochs: 300
eval_every: 10
early_stopping:
  patience: 20
  min_delta: 0.0001
```

### Через аргументы командной строки

```bash
# Можно добавить дополнительные аргументы в train_model.py
# Например: --learning_rate 0.0005 --batch_size 4096
```

---

## 📈 Результаты обучения

### Где сохраняются результаты

#### При использовании `train_model.py`:

1. **Чекпоинты моделей:**
   ```
   results/checkpoints/{model}/{dataset}/checkpoint_epoch_{epoch}.pt
   ```

2. **Метрики:**
   ```
   results/metrics/{model}_{dataset}.json
   ```

#### При использовании `run_all_experiments.py`:

1. **Все результаты (JSON):**
   ```
   results/experiments/all_experiments_{timestamp}.json
   ```

2. **Сводная таблица (CSV):**
   ```
   results/experiments/all_experiments_{timestamp}.csv
   ```

3. **Отдельные файлы для каждой комбинации:**
   ```
   results/experiments/{model}_{dataset}_results.json
   ```

4. **Чекпоинты моделей:**
   ```
   results/checkpoints/{model}/{dataset}/checkpoint_epoch_{epoch}.pt
   ```

### Формат результатов

**Файл `results/metrics/{model}_{dataset}.json`:**
```json
{
  "model": "lightgcn",
  "dataset": "movie_lens",
  "seed": 42,
  "best_epoch": 150,
  "best_metric": 0.0623,
  "training_time": 1234.56,
  "test_metrics": {
    "recall@10": 0.0623,
    "recall@20": 0.0891,
    "recall@50": 0.1345,
    "ndcg@10": 0.0456,
    "ndcg@20": 0.0523,
    "ndcg@50": 0.0612,
    "coverage": 0.1234
  }
}
```

---

## 🔍 Примеры использования

### Пример 1: Обучение LightGCN на MovieLens

```bash
python scripts/train_model.py --model lightgcn --dataset movie_lens
```

**Что происходит:**
1. Загружаются обработанные данные из `data/processed/movie_lens/`
2. Создается модель LightGCN с параметрами из `config/models/lightgcn.yaml`
3. Загружается конфигурация обучения из `config/training.yaml`
4. Модель обучается с BPR Loss
5. Валидация каждые 10 эпох
6. Early stopping при отсутствии улучшения 20 эпох
7. Сохранение лучшего чекпоинта
8. Финальная оценка на test set

### Пример 2: Обучение GroupShuffleGNN на Book-Crossing

```bash
python scripts/train_model.py --model groupshuffle_gnn --dataset book_crossing
```

**Особенности:**
- Использует ортогональные преобразования
- Требует `embedding_dim` кратный `block_size` (по умолчанию 64 и 8)
- Параметры настраиваются в `config/models/groupshuffle_gnn.yaml`

### Пример 3: ⭐ Запуск всех моделей на всех датасетах (РЕКОМЕНДУЕТСЯ)

```bash
# Запустить все модели на всех датасетах
python scripts/run_all_experiments.py

# Запустить только определенные модели
python scripts/run_all_experiments.py --models lightgcn groupshuffle_gnn

# Запустить только на определенных датасетах
python scripts/run_all_experiments.py --datasets movie_lens book_crossing

# С другим seed
python scripts/run_all_experiments.py --seed 123

# Пропустить уже обученные модели
python scripts/run_all_experiments.py --skip_existing
```

**Что происходит:**
1. Запускаются все комбинации моделей и датасетов
2. Каждая модель обучается на каждом датасете
3. Результаты сохраняются в:
   - `results/experiments/all_experiments_{timestamp}.json` - все результаты
   - `results/experiments/all_experiments_{timestamp}.csv` - сводная таблица
   - `results/experiments/{model}_{dataset}_results.json` - отдельные файлы
4. В конце выводится сводка с лучшими результатами

**Преимущества:**
- ✅ Один запуск - все эксперименты
- ✅ Автоматическое сохранение результатов
- ✅ Сводная таблица для анализа
- ✅ Можно продолжить с `--skip_existing`

---

## 🐛 Решение проблем

### Проблема 1: "Обработанные данные не найдены"

**Решение:**
```bash
# Сначала подготовьте данные
python scripts/prepare_data.py --dataset movie_lens
```

### Проблема 2: "CUDA out of memory"

**Решение:**
- Уменьшите `batch_size` в `config/training.yaml`
- Используйте CPU: установите `device='cpu'` в `trainer.py`

### Проблема 3: "Модель не найдена"

**Решение:**
- Проверьте правильность названия модели: `python scripts/train_model.py --model lightgcn --dataset movie_lens`
- Доступные модели: `bpr_mf`, `lightgcn`, `gcnii`, `dgr`, `svd_gcn`, `layergcn`, `groupshuffle_gnn`

### Проблема 4: Медленное обучение

**Решение:**
- Используйте GPU (если доступно)
- Увеличьте `batch_size` (если позволяет память)
- Уменьшите `eval_every` для реже валидации

---

## 📊 Мониторинг обучения

### Во время обучения выводится:

```
Epoch   1/300 | Train Loss: 0.6931 | Recall@10: 0.0123 | NDCG@10: 0.0089
Epoch  10/300 | Train Loss: 0.4521 | Recall@10: 0.0234 | NDCG@10: 0.0156
Epoch  20/300 | Train Loss: 0.3210 | Recall@10: 0.0345 | NDCG@10: 0.0234
...
```

### После обучения:

```
ФИНАЛЬНЫЕ МЕТРИКИ НА TEST SET:
  recall@10           : 0.0623
  recall@20           : 0.0891
  recall@50           : 0.1345
  ndcg@10             : 0.0456
  ndcg@20             : 0.0523
  ndcg@50             : 0.0612
  coverage            : 0.1234
```

---

## 🎯 Следующие шаги

После обучения моделей:

1. **Сравнение результатов:**
   - Соберите метрики всех моделей
   - Сравните по Recall@K и NDCG@K

2. **Анализ over-smoothing:**
   - Используйте `evaluation/oversmoothing.py` (будет создан позже)

3. **Визуализация:**
   - Создайте графики сравнения моделей
   - Визуализируйте метрики по эпохам

4. **Статистические тесты:**
   - Запустите несколько раз с разными seeds
   - Вычислите mean ± std
   - Проведите t-test

---

## 💡 Советы

1. **Начните с простой модели:** `bpr_mf` или `lightgcn`
2. **Используйте маленький датасет:** сначала `movie_lens` (меньше данных)
3. **Проверьте конфигурацию:** убедитесь, что параметры разумные
4. **Мониторьте метрики:** следите за Recall@10 во время обучения
5. **Сохраняйте чекпоинты:** лучшая модель сохраняется автоматически

---

## 📚 Дополнительная информация

- **Архитектура моделей:** `src/models/MODELS_GUIDE.md`
- **Подготовка данных:** `src/data/README.md`
- **Конфигурации:** `config/models/` и `config/training.yaml`

---

**Удачи в обучении! 🚀**

